networks:
    sqlgateway:
      external: true
      name:  sqlgateway   

services:
   cache:
     build: 
       context: .
       dockerfile: Dockerfile6
     restart: always
     ports:
       - 51972:1972
       - 57772:57772
     volumes:
       - ./:/opt/cacheapp
     networks:
       - sqlgateway
